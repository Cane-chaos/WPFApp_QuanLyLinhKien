<UserControl x:Class="WPFApp_QuanLyLinhKien.Views.Admin.QLDonHangView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 mc:Ignorable="d" d:DesignWidth="1000" d:DesignHeight="600" Padding="10">
	<Grid RowDefinitions="Auto,*" ColumnDefinitions="2*,3*" Background="#fafafa">
		<!-- Header -->
		<Border Grid.ColumnSpan="2" Background="#009688" Padding="10" CornerRadius="6" Margin="0,0,0,10">
			<TextBlock Text="Quản lý Đơn hàng" Foreground="White" FontSize="20" FontWeight="Bold" />
		</Border>

		<!-- Left panel: Products + current editing details -->
		<ScrollViewer VerticalScrollBarVisibility="Auto">
			<StackPanel Spacing="10">
				<StackPanel>
					<TextBlock Text="Sản phẩm" FontWeight="Bold" FontSize="14" Margin="0,0,0,4" />
					<ItemsControl ItemsSource="{Binding SanPhams}">
						<ItemsControl.ItemTemplate>
							<DataTemplate>
								<Border BorderBrush="#dddddd" BorderThickness="1" CornerRadius="4" Padding="6" Margin="0,4,0,0" Background="White">
									<DockPanel>
										<Button Content="+" Width="30" Height="30" DockPanel.Dock="Right"
												Command="{Binding DataContext.AddItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
												CommandParameter="{Binding}" />
										<StackPanel>
											<TextBlock Text="{Binding TenSP}" FontWeight="SemiBold" />
											<TextBlock Text="{Binding GiaBan, StringFormat={}{0:N0}₫}" Foreground="#555" FontSize="12" />
										</StackPanel>
									</DockPanel>
								</Border>
							</DataTemplate>
						</ItemsControl.ItemTemplate>
					</ItemsControl>
				</StackPanel>
				<Separator />
				<TextBlock Text="Chi tiết đơn đang soạn" FontWeight="Bold" FontSize="14" />
				<DataGrid ItemsSource="{Binding CurrentEditingItems}" AutoGenerateColumns="False" CanUserAddRows="False" HeadersVisibility="Column" Height="220">
					<DataGrid.Columns>
						<DataGridTextColumn Header="Sản phẩm" Binding="{Binding SanPham.TenSP}" Width="*"/>
						<DataGridTextColumn Header="SL" Binding="{Binding SoLuong}" Width="60"/>
						<DataGridTextColumn Header="Đơn giá" Binding="{Binding DonGia, StringFormat={}{0:N0}}" Width="100"/>
						<DataGridTextColumn Header="Thành tiền" Binding="{Binding DonGia, StringFormat={}{0:N0}}" Width="110"/>
						<DataGridTemplateColumn Header="Xóa" Width="55">
							<DataGridTemplateColumn.CellTemplate>
								<DataTemplate>
									<Button Content="X" Command="{Binding DataContext.RemoveItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" />
								</DataTemplate>
							</DataGridTemplateColumn.CellTemplate>
						</DataGridTemplateColumn>
					</DataGrid.Columns>
				</DataGrid>
				<StackPanel Orientation="Horizontal" VerticalAlignment="Center" Spacing="12">
					<TextBlock Text="Tổng:" FontWeight="Bold" />
					<TextBlock Text="{Binding CurrentEditing.TongTien, StringFormat={}{0:N0}₫}" FontSize="16" Foreground="#d32f2f" FontWeight="Bold" />
					<Button Content="Tính lại" Command="{Binding RecalcCommand}" />
				</StackPanel>
				<StackPanel Orientation="Horizontal" Spacing="8" Margin="0,6,0,0">
					<Button Content="Thêm đơn" Command="{Binding AddHoaDonCommand}" />
					<Button Content="Lưu sửa" Command="{Binding SaveHoaDonCommand}" />
					<Button Content="Hủy chọn" Command="{Binding DeleteHoaDonCommand}" />
				</StackPanel>
			</StackPanel>
		</ScrollViewer>

		<!-- Right panel: Orders list -->
		<DockPanel Grid.Column="1" Margin="10,0,0,0">
			<TextBlock Text="Danh sách đơn hàng" FontWeight="Bold" FontSize="14" Margin="0,0,0,6" />
			<DataGrid ItemsSource="{Binding HoaDons}" SelectedItem="{Binding SelectedHoaDon}" AutoGenerateColumns="False" CanUserAddRows="False" HeadersVisibility="Column">
				<DataGrid.Columns>
					<DataGridTextColumn Header="Mã" Binding="{Binding MaHD}" Width="60" />
					<DataGridTextColumn Header="Ngày" Binding="{Binding NgayLap}" Width="150" />
					<DataGridTextColumn Header="Tổng" Binding="{Binding TongTien, StringFormat={}{0:N0}}" Width="120" />
				</DataGrid.Columns>
			</DataGrid>
			<StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0" Spacing="8">
				<Button Content="Sửa" Command="{Binding StartEditHoaDonCommand}" />
				<Button Content="Xóa" Command="{Binding DeleteHoaDonCommand}" />
			</StackPanel>
		</DockPanel>
	</Grid>
</UserControl>
