<UserControl x:Class="WPFApp_QuanLyLinhKien.Views.Admin.BaoCaoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Padding="10">

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>

		<!-- Tiêu đề -->
		<Border Background="#ff9800" Padding="8" CornerRadius="4" Margin="0,0,0,10">
			<TextBlock Text="Báo cáo tổng quan"
                       Foreground="White"
                       FontSize="18"
                       FontWeight="Bold" />
		</Border>

		<!-- Nội dung báo cáo -->
		<UniformGrid Grid.Row="1" Rows="2" Columns="2" Margin="0">
			<Border BorderBrush="#ddd" BorderThickness="1" Margin="6" Padding="12" CornerRadius="6" Background="White">
				<StackPanel>
					<TextBlock Text="Tổng khách hàng" FontWeight="Bold" />
					<TextBlock Text="{Binding TongSoKhachHang}" FontSize="24" Foreground="#3f51b5" />
				</StackPanel>
			</Border>

			<Border BorderBrush="#ddd" BorderThickness="1" Margin="6" Padding="12" CornerRadius="6" Background="White">
				<StackPanel>
					<TextBlock Text="Tổng đơn hàng" FontWeight="Bold" />
					<TextBlock Text="{Binding TongSoDonHang}" FontSize="24" Foreground="#009688" />
				</StackPanel>
			</Border>

			<Border BorderBrush="#ddd" BorderThickness="1" Margin="6" Padding="12" CornerRadius="6" Background="White">
				<StackPanel>
					<TextBlock Text="Doanh thu" FontWeight="Bold" />
					<TextBlock Text="{Binding TongDoanhThu, StringFormat={}{0:N0}₫}" FontSize="24" Foreground="#e91e63" />
				</StackPanel>
			</Border>

			<Border BorderBrush="#ddd" BorderThickness="1" Margin="6" Padding="12" CornerRadius="6" Background="White">
				<StackPanel>
					<TextBlock Text="Ghi chú" FontWeight="Bold" />
					<TextBlock Text="Demo dữ liệu nội bộ" TextWrapping="Wrap" />
				</StackPanel>
			</Border>
		</UniformGrid>
	</Grid>
</UserControl>
